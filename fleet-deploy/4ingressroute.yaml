apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: web-app
  namespace: testing-deployment
spec:
  entryPoints:
    - app1
  routes:
    - match: PathPrefix(`/`) 
      kind: Rule
      services:
        - name: laravel-filament
          port: 80
      middlewares:
        - name: change-header